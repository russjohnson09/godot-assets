This repo contains references to various assets used in some godot projects.


origin  git@github.com:russjohnson09/zxa-platform.git (fetch)


The assets directory structure is preserved but the individual assets are included based on the need of the specific project.

There are a few scripts that do things like take a youtube video and split it into multiple parts and place those parts in the assets directory.


Smaller assets are sometimes tracked directly in the git repo itself if small enough.